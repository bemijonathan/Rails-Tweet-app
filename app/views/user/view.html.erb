<div class="container"> 
    <div>
    <% if @users.length != 0 %>
        <% @users.each do |user| %>
            <div class="list">
                <%# <p><%= link_to("#{user.content}" , "/users/#{user.id}") %>
                <%# </p> %>
                <%= "#{user.name}" %> <br>
                <%= user.email %>
                <%= link_to("Message", "/chats/#{user.id}")%>
            </div>
        <% end %>
    <% else%>
        "No user to display"
    <% end %>
    <div>
</div>